const height_cm = 170
const height_m = 1.70
const weight_kg = 80.2

const height_red = Math.ceil(height_m)
const weight_red = Math.floor(weight_kg)
console.log(height_red , weight_red)

const sonIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE
console.log(sonIguales)
